This is a front-end that connects to a previously built back-end API.

To run locally:
1)Fork repo
2)Clone repo locally
3)Run "npm start" in the terminal, this will open up a local version of the webpage.

Link to the hosted version:
